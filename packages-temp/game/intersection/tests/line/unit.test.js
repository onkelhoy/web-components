import { describe, it, beforeEach } from "node:test";
import assert from "node:assert";

// import { LineIntersection, SegmentIntersection } from '@papit/game-intersection';

describe.skip('@papit/game-intersection [line] unit tests', () => {
  // beforeEach(() => console.log('about to run a test'));

  it('template test', () => {
    // assert.strictEqual(1, 1);
  });
});