// core
import '@papit/core';
import '@papit/translator';
import "@papit/codeblock";
import "@papit/typography";

// component
import '@papit/card';

window.onload = () => {
  console.log('[demo]: window loaded');
}
