import { describe, it, beforeEach } from "node:test";
import assert from "node:assert";

// import { GameMatrix } from '@papit/game-matrix';

describe.skip('@papit/game-matrix unit tests', () => {
  // beforeEach(() => console.log('about to run a test'));

  it('template test', () => {
    // const temp = new GameMatrix();
    // temp.doSomething();
    // assert.strictEqual(1, 1);
  });
});